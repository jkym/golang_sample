machine:
  timezone:
    Asia/Tokyo
dependencies:
  pre:
    - go get github.com/revel/revel
    - go get github.com/revel/cmd/revel
    - export PATH="$PATH:$GOPATH/bin"
    - ln -s "`pwd`/golang_sample" $GOPATH
test:
  override:
    - revel test myapp
